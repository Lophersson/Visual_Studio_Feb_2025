string[] nombres = { "Luis", "Jose", "María", "Elena" };
string nombre = "Lucía";

Console.WriteLine(nombres[2]);  // Imprime "María"
Console.WriteLine(nombres.Length); // Imprime la cantidad de nombres en el array

int NumeroTotal = 0;

// Primer bucle: Imprimir los caracteres de "nombre" y su longitud
for (int i = 0; i < nombre.Length; i++) {
    Console.WriteLine(nombre[i]);        // Imprime cada carácter de "Lucía"
    Console.WriteLine(nombre[i].ToString().Length); // Imprime la longitud de cada carácter (siendo siempre 1)
}

// Segundo bucle: Imprimir la longitud de cada nombre
for (int i = 0; i < nombres.Length; i++) {
    Console.WriteLine(nombres[i].Length); // Imprime la longitud de cada nombre
}

// Tercer bucle: Sumar la longitud de todos los nombres
for (int i = 0; i < nombres.Length; i++) {
    NumeroTotal = NumeroTotal + nombres[i].Length; // Sumar la longitud de cada nombre
}

// Imprimir el total de caracteres
Console.WriteLine("Total de caracteres en todos los nombres: " + NumeroTotal);
---
string[] nombres = { "Luis", "Jose", "María", "Elena" };
string nombre = "Lucía";

Console.WriteLine(nombres[2]);  // Imprime "María"
Console.WriteLine(nombres.Length); // Imprime la cantidad de nombres en el array

int NumeroTotal = 0;

// Imprimir la longitud de cada nombre
for (int i = 0; i < nombres.Length; i++) {
    Console.WriteLine(nombres[i].Length); // Imprime la longitud de cada nombre
}

// Sumar la longitud de todos los nombres
for (int i = 0; i < nombres.Length; i++) {
    NumeroTotal = NumeroTotal + nombres[i].Length; // Sumar la longitud de cada nombre
}

// Imprimir el total de caracteres
Console.WriteLine("Total de caracteres en todos los nombres: " + NumeroTotal);

// Imprimir los caracteres de "nombre" y su longitud
for (int i = 0; i < nombre.Length; i++) {
    Console.WriteLine(nombre[i]);        // Imprime cada carácter de "Lucía"
    Console.WriteLine(nombre[i].ToString().Length); // Imprime la longitud de cada carácter (siendo siempre 1)
}

// Línea de separación
Console.WriteLine("---------");

Console.WriteLine(string.Join(", ", nombres)); // Imprime todos los nombres en una sola línea

// Imprimir la longitud de cada nombre en la siguiente línea
for (int i = 0; i < nombres.Length; i++) {
    Console.Write(nombres[i].Length + " "); // Imprime la longitud de cada nombre en la misma línea
}

Console.WriteLine(); // Añade una nueva línea al final
---
string[] nombres = { "Luis", "Jose", "María", "Elena" };
string nombre = "Lucía";

int NumeroTotal = 0;

Console.WriteLine(string.Join(", ", nombres)); // Imprime todos los nombres en una sola línea

// Imprimir la longitud de cada nombre en la siguiente línea
for (int i = 0; i < nombres.Length; i++) {
    Console.Write(nombres[i].Length + ", "); // Imprime la longitud de cada nombre en la misma línea
}

// Línea de separación
Console.WriteLine("---------");
---
string[] nombres = { "Luis", "Jose", "María", "Elena" };
string nombre = "Lucía";

int NumeroTotal = 0;

Console.WriteLine(string.Join(", ", nombres)); // Imprime todos los nombres en una sola línea

// Imprimir la longitud de cada nombre en la siguiente línea
for (int i = 0; i < nombres.Length; i++) {
    Console.Write(nombres[i].Length + ", "); // Imprime la longitud de cada nombre en la misma línea
}

// Línea de separación
Console.WriteLine("---------");
---
string[] nombres = { "Luis", "Jose", "María", "Elena" };
string nombre = "Lucía";

List<string> MiLista = new List<string>();
        MiLista.Add("OSCAR");
        MiLista.Add("PEDRO");
        MiLista.Add("MARIA");

        foreach (string NombrePersona in MiLista) {
            Console.WriteLine(NombrePersona); // Imprime cada nombre en la lista
        }
---

string[] nombres = { "Luis", "Jose", "María", "Elena" };
string nombre = "Lucía";

List<string> MiLista = new List<string>();
        MiLista.Add("OSCAR");
        MiLista.Add("PEDRO");
        MiLista.Add("MARIA");

        foreach (string NombrePersona in MiLista) {
            Console.WriteLine(NombrePersona); // Imprime cada nombre en la lista
        }
---
